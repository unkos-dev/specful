# tests/fixtures holds repositories that are deliberately malformed.
[global]
exclude = ["target", "tests/fixtures"]

[MD004]
style = "dash"

# Front matter `title:` is metadata, not a rendered heading.
[MD025]
front-matter-title = ""

[MD013]
line-length = 120
headings = true
code-blocks = false
tables = true
